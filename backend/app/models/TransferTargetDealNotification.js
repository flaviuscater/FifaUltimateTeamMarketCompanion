class TransferTargetDealNotification {

    transferTarget;
    console;

    constructor(transferTarget, console) {
        this.transferTarget = transferTarget;
        this.console = console;
    }
}